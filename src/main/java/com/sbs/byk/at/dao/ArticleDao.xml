<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sbs.byk.at.dao.ArticleDao">
	<select id="getForPrintArticles" parameterType="String"
		resultType="Article">
		SELECT *
		FROM article
		WHERE displayStatus = 1
		AND delStatus !=
		1
		<if
			test="searchKeyword != null and searchKeywordType.equals('title') ">
			AND title LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		ORDER BY id DESC
		LIMIT #{limitFrom}, #{itemsInAPage}
	</select>

	<select id="getOne" parameterType="int" resultType="Article">
		SELECT *
		FROM
		article
		WHERE displayStatus = 1
		AND id = #{id}
	</select>

	<insert id="write" useGeneratedKeys="true" keyProperty="id">
		INSERT
		INTO article
		SET regDate = NOW(),
		updateDate = NOW(),
		displayStatus = 1,
		title = #{title},
		body = #{body}
	</insert>

	<update id="modify" parameterType="map">
		UPDATE article
		SET title =
		#{title},
		body = #{body},
		updateDate = NOW()
		WHERE id = #{id}
	</update>

	<update id="delete" parameterType="int">
		UPDATE article
		SET delStatus =
		1,
		delDate = NOW(),
		displayStatus = 0
		WHERE id =
		#{id}
	</update>

	<select id="getFirstIdFromArticle" resultType="int">
		SELECT id
		FROM
		article
		WHERE displayStatus = 1
		AND delStatus != 1
		ORDER BY id ASC
		LIMIT 1
	</select>

	<select id="getListIdFromArticle" resultType="int">
		SELECT id
		FROM
		article
		WHERE displayStatus = 1
		AND delStatus != 1
		ORDER BY id DESC
		LIMIT
		1
	</select>

	<select id="getNextArticle" parameterType="int"
		resultType="Article">
		<![CDATA[
		SELECT *
		FROM article
		WHERE displayStatus = 1
		AND delStatus != 1
		AND id > #{id}
		ORDER BY id ASC
		LIMIT 1
		 ]]>
	</select>

	<select id="getPreviousArticle" parameterType="int"
		resultType="Article">
		<![CDATA[
		SELECT *
		FROM article
		WHERE displayStatus = 1
		AND delStatus != 1
		AND id < #{id}
		ORDER BY id DESC
		LIMIT 1
		 ]]>
	</select>

	<select id="getTotalCount" parameterType="String"
		resultType="int">
		SELECT COUNT(*)
		FROM
		article
		WHERE displayStatus = 1
		AND delStatus != 1
		<if
			test="searchKeyword != null and searchKeywordType.equals('title') ">
			AND title LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
	</select>

	<insert id="writeReply" useGeneratedKeys="true" keyProperty="id">
		INSERT
		INTO articleReply
		SET regDate = NOW(),
		updateDate = NOW(),
		displayStatus = 1,
		articleId = #{articleId},
		body = #{body}
	</insert>

	<select id="getForPrintArticleReplies" parameterType="int"
		resultType="ArticleReply">
		SELECT * FROM
		articleReply
		WHERE displayStatus = 1
		AND
		delStatus != 1
		AND articleId = #{articleId}
		ORDER BY id DESC
	</select>

	<select id="getForPrintArticleRepliesFrom"
		resultType="ArticleReply">
		SELECT * FROM
		articleReply
		WHERE displayStatus = 1
		AND
		delStatus != 1
		AND articleId = #{articleId}
		AND id >= #{from}
		ORDER BY id
		DESC
	</select>

	<update id="deleteReply" parameterType="int">
		UPDATE articleReply
		SET
		delStatus =
		1,
		delDate = NOW(),
		displayStatus = 0
		WHERE id =
		#{id}
	</update>

	<select id="getArticleReplyById" parameterType="int"
		resultType="ArticleReply">
		SELECT *
		FROM
		articleReply
		WHERE displayStatus = 1
		AND
		delStatus != 1
		AND id = #{id}
	</select>

	<update id="modifyReply" parameterType="map">
		UPDATE articleReply
		SET
		body = #{body},
		updateDate = NOW()
		WHERE id = #{id}
	</update>

	<update id="deleteArticleReply">
		UPDATE articleReply
		SET
		delStatus =
		1,
		delDate = NOW(),
		displayStatus = 0
		WHERE id =
		#{id}
	</update>

	<update id="modifyArticleReply">
		UPDATE articleReply
		SET
		body = #{body},
		updateDate = NOW()
		WHERE id = #{id}
	</update>
</mapper>